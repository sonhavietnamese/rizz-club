'use client'

import LoggedOutView from '@/components/LoggedOutView'
import LoggedInView from '@/components/login'
import { useWepin } from '@/lib/wepin'

export default function Page() {
  return (
    <main className="w-full h-screen flex items-center justify-center gap-4"></main>
  )
}
